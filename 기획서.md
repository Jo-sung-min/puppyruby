# PuppyRuby 도트 강아지 염색·액세서리 확장 기획서

> 작성일: 2026-09-21  
> 상태: 아키타 한정 1차 시범 구현 완료, 전체 견종 전환은 스타일 확정 후 진행  
> 적용 대상: Ruby Round 도트 강아지, 웹, Windows 실행파일, 관리자

## 시범 적용 현황 (2026-09-22)

현재 사용 중인 아키타의 16동작에 털 마스크와 공통 팔레트를 추가했다. 웹과 새 Windows 빌드는 동일한 염색 계산을 사용하며, 눈은 기존 분리 합성을 유지한다. 관리자에는 프레임별 액세서리 위치를 편집하고 저장하는 로컬 작업실을 추가했다. 다른 29견종과 기존 출시 에셋은 유지한다.

이 문서의 나머지 항목은 최종 확장 목표다. 방향별 액세서리 원화, 복수 슬롯 동시 장착, 운영용 자산 업로드·승인, 상품 판매 기능은 이번 시범 범위에 포함하지 않는다. 배포·운영 S3 게시와 설치된 프로그램의 교체도 수행하지 않았다. 실제 파일, 검증 방법, CDN 전환 조건은 [아키타 시범 적용 안내](docs/AKITA_COAT_PILOT.md)에 정리했다.

## 1. 기획 목적

현재 Ruby Round 강아지는 Aseprite에서 만든 프레임을 PNG 스프라이트 시트로 내보내 웹과 Windows 실행파일에서 재생한다. 이 방식 자체는 정상적인 픽셀 애니메이션 구조이지만, 몸통 PNG 안에 털색·무늬·고정 요소가 함께 합쳐져 있어 염색 색상마다 완성 이미지를 다시 만들게 될 위험이 있다.

이 기획의 목적은 다음과 같다.

- 30견종 × 16동작 × 4프레임의 기본 강아지는 한 번만 제작한다.
- 새 염색은 강아지 이미지를 다시 만들지 않고 팔레트 데이터만 추가한다.
- 새 액세서리는 강아지가 포함된 완성 이미지를 만들지 않고 공통 투명 PNG와 위치값으로 적용한다.
- 웹과 Windows 실행파일이 같은 이미지, 팔레트, 좌표를 사용해 동일하게 보이게 한다.
- 기존 사용자 데이터와 구버전 실행파일의 호환성을 유지한다.

## 2. 현재 구조와 문제점

### 현재 자산

- 30견종
- 16동작
- 동작당 4프레임
- 총 480개 동작 시트, 1,920개 원본 프레임
- Aseprite는 편집·검수용 원본이며 런타임은 내보낸 PNG를 사용한다.
- 눈은 몸통과 별도로 합성된다.
- 액세서리는 `face`, `head`, `neck`, `back` 슬롯과 앞·뒤 레이어 구조를 이미 일부 지원한다.

### 해결할 문제

1. Ruby Round 렌더러는 현재 저장된 털색 값을 실제 몸통 PNG에 적용하지 않는다.
2. 몸통 PNG에서 털, 윤곽선, 코, 입, 혀, 발바닥과 무늬가 의미별로 구분되지 않는다.
3. 전체 이미지에 색상 필터를 적용하면 눈·코·혀·윤곽선까지 변하며 도트 명암이 깨진다.
4. 액세서리 위치는 기본 5장면 중심으로 검수되어 있고 나머지 11동작은 정확한 슬롯 좌표가 부족하다.
5. 정면 액세서리 한 장만으로 옆모습·뒷모습·누운 모습까지 자연스럽게 표현하기 어렵다.

## 3. 핵심 원칙

### 완성 이미지 조합을 만들지 않는다

`강아지 × 염색 × 눈 × 액세서리` 조합별 PNG를 만들지 않는다. 런타임에서 독립된 레이어를 합성한다.

```text
뒤쪽 액세서리
  ↓
강아지 몸통 + 선택한 염색 팔레트
  ↓
선택한 눈과 표정
  ↓
소품과 앞쪽 액세서리
  ↓
하트·말풍선 같은 효과
```

### 도트 원본을 보존한다

- 모든 위치와 크기는 정수 픽셀을 사용한다.
- 확대·축소는 nearest-neighbor 방식만 허용한다.
- 염색은 지정된 털 영역만 바꾼다.
- 윤곽선과 얼굴 부위는 원본 색을 유지한다.
- 프레임 재생 중 매번 색을 계산하지 않고 선택 변경 시 한 번 합성한 결과를 캐시한다.

## 4. Aseprite 제작 규격

모든 견종의 64프레임 Aseprite 원본은 같은 의미 구조를 사용한다.

```text
Dog
├─ FixedBack
├─ Coat
│  ├─ PrimaryTone
│  ├─ SecondaryTone
│  └─ MarkingTone
├─ FixedFront
├─ EyeGuide
└─ Anchors
   ├─ Face
   ├─ Head
   ├─ Neck
   └─ Back
```

| 그룹 | 포함 항목 | 염색 여부 |
|---|---|---:|
| `FixedBack` | 몸 뒤에 있어야 하는 고정 소품과 그림자 | 불가 |
| `PrimaryTone` | 몸의 주 털 영역과 명암 | 가능 |
| `SecondaryTone` | 배, 주둥이, 귀 등 보조 털 영역과 명암 | 가능 |
| `MarkingTone` | 반점, 마스크, 줄무늬 등 견종 무늬 | 가능 |
| `FixedFront` | 윤곽선, 코, 입, 혀, 발바닥, 키보드·밥그릇·손 등의 앞 소품 | 불가 |
| `EyeGuide` | 공통 눈을 합성할 위치와 눈 표시 방식 | 해당 없음 |
| `Anchors` | 액세서리 슬롯의 기준점 | 해당 없음 |

눈은 현재처럼 공통 눈 에셋을 유지한다. 감은 눈이 본체에 포함된 프레임과 눈이 보이지 않는 뒷모습은 기존 `shared`, `baked-closed`, `hidden` 규칙을 유지한다.

## 5. 내보내기 결과물

각 견종과 동작은 아래 결과물을 만든다.

| 파일 | 역할 |
|---|---|
| `body-sheet.png` | 원본 몸통, 고정 영역, 소품을 포함한 4프레임 시트 |
| `coat-map.png` | 몸통과 같은 크기의 염색 역할·명암 마스크 |
| `anchors.json` | 16동작 × 4프레임의 눈과 액세서리 위치 |
| `conversion.json` | 원본·출력 해시, 크기, 프레임, 검수 이력 |

`coat-map.png`는 다음 정보를 담는다.

- 재질 역할: 주 털, 보조 털, 무늬
- 원본의 명암값: 0~255
- 픽셀 적용 범위와 투명도
- 염색하지 않는 영역은 적용 범위 0

마스크는 사용자에게 직접 표시하는 그림이 아니라 웹과 Windows 합성기가 읽는 데이터 이미지다.

## 6. 염색 시스템

### 팔레트 카탈로그

염색은 PNG가 아닌 공통 JSON 카탈로그로 관리한다. 제안 파일명은 `shared/coat-palettes.json`이다.

```json
{
  "schemaVersion": 1,
  "revision": "coat-palettes-1",
  "items": [
    {
      "id": "strawberry-milk",
      "label": "딸기 우유",
      "primary": {
        "shadow": "#8F5E56",
        "mid": "#D99A94",
        "highlight": "#FFD0CC"
      },
      "secondary": {
        "shadow": "#B77D77",
        "mid": "#F2B8B2",
        "highlight": "#FFE2DE"
      },
      "marking": {
        "shadow": "#704942",
        "mid": "#A96E65",
        "highlight": "#EAB2A8"
      }
    }
  ]
}
```

### 합성 방식

1. 몸통 시트와 같은 크기의 염색 마스크를 읽는다.
2. 마스크가 주 털인 픽셀에는 `primary` 팔레트를 적용한다.
3. 보조 털과 무늬도 각 팔레트의 명암 범위로 변환한다.
4. 고정 영역은 원본 픽셀을 그대로 사용한다.
5. 결과를 `몸통 해시 + 마스크 해시 + 팔레트 revision` 키로 캐시한다.

웹은 `OffscreenCanvas` 또는 Canvas worker에서 한 번 합성하고, Windows는 `Bitmap.LockBits` 방식으로 같은 정수 계산을 사용한다. 화면을 그릴 때마다 픽셀을 다시 변환해서는 안 된다.

### 저장 방식

첫 단계에서는 기존 강아지의 `fur` 값에 색상 코드 대신 팔레트 ID를 저장한다. 백엔드의 고정 색상 목록 검증을 팔레트 카탈로그 검증으로 변경하면 DB 스키마 변경 없이 적용할 수 있다.

향후 주 털과 보조 털을 사용자가 각각 선택하게 되면 아래 중 하나로 확장한다.

- `primaryPaletteId`, `secondaryPaletteId`, `markingPaletteId` 필드 추가
- 또는 버전이 있는 `appearance` JSON 객체 추가

### 무늬 확장

단순 염색은 기존 무늬의 색만 바꾼다. 반점 위치나 턱시도 모양처럼 형태 자체를 바꾸려면 별도의 `pattern-mask`가 필요하다. 패턴도 몸통 완성 이미지를 다시 만들지 않고, 동작에 붙어 움직이는 재사용 마스크로 관리한다.

## 7. 액세서리 시스템

### 기본 구조

액세서리 하나는 다음 요소로 구성한다.

- 투명 PNG 또는 4프레임 스프라이트 시트
- 고유 ID와 revision
- 기준점인 pivot
- 부착 슬롯
- 몸 뒤 또는 몸 앞 레이어
- 방향별 이미지 보기
- 필요한 프레임만 기록하는 예외 위치값

강아지 DB에는 액세서리 ID만 저장한다. 이미지와 위치를 수정해도 회원·강아지 테이블을 변경하지 않는다.

현재 강아지 데이터는 액세서리 ID 하나만 저장하므로 1차 적용은 한 번에 하나만 장착하는 기존 동작을 유지한다. 이후 모자와 안경처럼 여러 슬롯을 동시에 장착하려면 UI만 늘리지 않고, `equippedAccessories`를 슬롯별 목록으로 저장하는 별도 관계 또는 버전이 있는 appearance 객체를 추가한다. 같은 슬롯에는 기본적으로 한 개만 허용하고, 서로 겹치는 품목의 우선순위와 최대 장착 수는 서버에서도 동일하게 검증한다.

### 슬롯

| 슬롯 | 예시 |
|---|---|
| `face` | 안경, 고글 |
| `head` | 리본, 모자, 왕관, 꽃 |
| `neck` | 목걸이, 목도리 |
| `back` | 날개, 망토 |

모든 30견종에 대해 16동작 × 4프레임의 슬롯 위치를 저장한다. 각 위치는 다음 값을 가진다.

```json
{
  "x": 148,
  "y": 161,
  "width": 104,
  "height": 48,
  "rotation": 0,
  "flipX": false,
  "visible": true,
  "view": "front"
}
```

### 액세서리 유형별 제작 기준

| 유형 | 권장 자산 수 | 적용 방식 |
|---|---:|---|
| 안경, 리본, 왕관 | 1~2개 보기 | 공통 이미지와 슬롯 변환 |
| 목도리, 목걸이 | 2~4개 보기 | 정면·옆면 이미지, 필요 시 앞·뒤 레이어 분리 |
| 날개, 망토 | 3~4개 보기 | 정면·옆·뒤 보기 선택 |
| 상의, 전신 옷 | 체형별 4~6개 템플릿 | 소형·긴몸·넓은몸·복슬몸 등 fit profile 사용 |

`front`, `side-left`, `side-right`, `back`, `supine` 보기 중 필요한 것만 만든다. 한 액세서리를 30견종 수만큼 복제하지 않는다. 귀가 매우 크거나 주둥이가 긴 견종처럼 공통 슬롯으로 맞지 않는 경우에만 희소한 견종·동작·프레임 예외값을 저장한다.

### 가림과 레이어

목도리처럼 몸을 감싸는 액세서리는 한 이미지를 억지로 올리지 않고 `behind`와 `front` 두 조각으로 나눈다. 프레임별로 보이지 않아야 하는 방향은 `visible: false`로 처리한다.

## 8. 런타임 합성 순서

웹과 Windows 실행파일은 아래 순서를 동일하게 사용한다.

1. `behind` 액세서리
2. 몸통의 뒤 고정 요소
3. 염색된 주 털·보조 털·무늬
4. 몸통의 앞 고정 요소
5. 선택한 눈과 시선 이동
6. `front` 액세서리
7. 하트, 말풍선, 동작 효과

다음 값이 하나라도 바뀌면 합성 캐시 키가 달라져야 한다.

- 몸통 이미지 해시
- 염색 마스크 해시
- 팔레트 revision
- 눈 revision
- 액세서리 이미지 해시와 revision
- 액세서리 앵커 revision

## 9. 관리자 기능

### 염색 관리

- 팔레트 추가·수정·비활성화
- 주 털·보조 털·무늬 색상 미리보기
- 밝은 털과 어두운 털에서 윤곽·명암 유지 확인
- 여러 견종과 대표 동작을 한 화면에서 비교
- 판매 여부, 가격, 공개일 설정

### 액세서리 관리

- 투명 PNG 업로드
- 슬롯, 앞뒤 레이어, 방향 보기 설정
- 프레임별 드래그 이동
- 크기, 회전, 좌우 반전, 표시 여부 조정
- 견종별 예외값만 저장
- 16동작 전체 미리보기
- 웹·Windows 공통 descriptor 확인

### 발행 관리

- 원본 크기, 투명도, 파일 크기, SHA-256 검사
- 검수 전 임시 저장과 검수 완료 상태 구분
- S3/CDN 업로드 후 실제 바이트와 해시 확인
- 활성 release를 바꾸기 전 전체 30견종 참조 확인
- 이전 release로 되돌릴 수 있는 rollback 정보 유지

## 10. 웹 구현 원칙

- CDN 이미지에 Canvas 접근이 필요하므로 허용된 서비스 도메인에 대한 CORS GET 설정을 확인한다.
- 염색 결과는 브라우저 메모리 캐시 또는 IndexedDB에 저장할 수 있다.
- 스프라이트 애니메이션은 기존 CSS `steps()` 프레임 재생을 유지한다.
- DOM에는 합성 완료된 한 장의 스프라이트 시트를 표시해 프레임마다 Canvas를 다시 그리지 않는다.
- 확대 표시에는 `image-rendering: pixelated`를 유지한다.

## 11. Windows 실행파일 구현 원칙

- 서버 descriptor가 제공하는 몸통·마스크·눈·액세서리를 SHA-256으로 검증한다.
- 염색은 이미지 수신 또는 사용자 선택 변경 시 한 번만 수행한다.
- 합성 결과를 render key로 디스크와 메모리에 캐시한다.
- 네트워크 연결이 없을 때는 설치파일에 포함된 기본 루비 도트와 기본 팔레트를 표시한다.
- 구버전 서버 응답이나 마스크가 없는 자산은 기존 원본 색상으로 안전하게 표시한다.

## 12. API·호환성 원칙

- 기존 5장면 필드는 구버전 실행파일을 위해 유지한다.
- 신버전 descriptor에 16동작 마스크, 전체 액세서리 앵커와 revision을 선택적으로 추가한다.
- 신버전 기능을 지원하는 클라이언트만 확장 필드를 받도록 appearance capability version을 사용한다.
- 제거된 팔레트나 액세서리를 저장한 기존 강아지는 기본 항목으로 대체하되 원래 ID는 감사 기록에 남긴다.
- 공개된 ID를 다른 상품에 재사용하지 않는다.
- 서버는 팔레트·액세서리의 활성 상태, 보유권과 강아지 소유권을 검증한다.
- 같은 저장 요청이 재시도되어도 결과가 중복 적용되지 않게 한다.
- 클라이언트가 보낸 카탈로그 revision이 오래된 경우 최신 카탈로그를 다시 받은 뒤 저장하게 한다.

## 13. 사용자 흐름과 상품 정책

### 옷장 흐름

1. 꾸밀 강아지를 선택한다.
2. 보유한 염색과 액세서리는 바로 미리 볼 수 있다.
3. 미보유 상품은 잠금과 획득 방법을 표시하고 미리보기만 허용한다.
4. 저장 시 서버가 보유권과 호환성을 다시 확인한다.
5. 저장이 완료되면 웹과 연결된 Windows 강아지가 같은 appearance revision을 받는다.
6. 원래 털색과 액세서리 장착 해제를 항상 선택할 수 있다.

로딩·저장 실패·품절·비활성 상품 상태를 각각 구분해 표시한다. 색상은 색상칩만 보여주지 않고 이름과 선택 상태도 함께 표시해 색각과 키보드 접근성을 확보한다.

### 유료 항목

- 무료 지급, 직접 구매, 뽑기 획득 여부를 카탈로그에 기록한다.
- 신규 유료 항목은 확률과 판매 상태를 비활성화한 채 먼저 배포한다.
- S3/CDN, 백엔드, 프런트, Windows 검증이 끝난 뒤 관리자가 가격·확률·판매 상태를 활성화한다.
- 중복 획득, 수량 누적, 환불 처리와 확률 공개는 기존 결제 정책을 따른다.
- 팔레트와 액세서리 ID는 판매 종료 후에도 기존 보유자가 사용할 수 있도록 유지한다.

## 14. 단계별 적용 계획

### 1단계: 아키타 염색 PoC

- 아키타 64프레임을 의미 레이어로 정리한다.
- 기본색 포함 4개 팔레트를 만든다.
- 몸통 시트와 염색 마스크를 내보낸다.
- 웹에서 16동작과 눈 30종을 조합해 검수한다.

### 2단계: 웹·Windows 일치

- 같은 정수 팔레트 계산을 양쪽에 적용한다.
- 기본색 결과가 기존 원본과 허용 범위 내에서 일치하는지 확인한다.
- 웹과 Windows의 대표 프레임을 픽셀 단위로 비교한다.
- 캐시와 오프라인 기본 자산을 검증한다.

### 3단계: 액세서리 16동작 rig

- 아키타의 16동작 × 4프레임에 네 슬롯을 기록한다.
- 실제 투명 PNG 안경 하나를 추가한다.
- 정면·옆·뒤·누운 모습의 위치와 가림을 검수한다.
- 관리자 좌표 편집기를 적용한다.

### 4단계: 30견종 확대

- Aseprite 내보내기와 검증을 자동화한다.
- 나머지 29견종의 염색 마스크와 16동작 슬롯을 만든다.
- 견종별 예외값은 필요한 프레임만 저장한다.
- 전체 자동 검증 후 수동 시각 검수를 완료한다.

### 5단계: 운영 발행

- 새 불변 S3/CDN release를 발행한다.
- 백엔드 JAR, Vercel 프런트, Windows 설치파일 순서로 호환성을 확인한다.
- 관리자에서 유료 팔레트와 액세서리를 최종 활성화한다.

## 15. 검수 기준

### 염색

- 코, 입, 혀, 발바닥, 눈, 윤곽선 색이 변하지 않는다.
- 원본의 그림자와 하이라이트 단계가 유지된다.
- 강아지 가장자리에 색 번짐이나 아우라가 없다.
- 네 프레임 사이에서 털색이 깜빡이지 않는다.
- 기본 팔레트는 기존 강아지 인상과 최대한 동일하다.

### 액세서리

- 16동작 전체에서 머리나 몸의 움직임을 따라간다.
- 정면·옆·뒤·누운 모습에 맞는 보기가 선택된다.
- 안경이 눈과 함께 이동하되 눈동자 움직임에는 끌려가지 않는다.
- 몸 뒤에 있어야 하는 부분과 앞에 있어야 하는 부분이 올바르게 가려진다.
- 프레임 경계에서 잘리거나 떨리지 않는다.

### 성능과 장애 대응

- 애니메이션 재생 중 프레임마다 염색 계산을 반복하지 않는다.
- 마스크나 액세서리 로딩에 실패해도 강아지 몸통은 표시된다.
- CDN 자산의 해시가 다르면 캐시를 사용하지 않는다.
- 구버전 데이터와 구버전 실행파일의 기본 장면이 계속 동작한다.
- 저장 후 재접속하거나 다른 기기에서 접속해도 같은 외형이 복원된다.
- 프런트 빌드, 백엔드 카탈로그 검증, Windows 캐시 무효화 검사가 모두 통과한다.

## 16. 첫 출시에서 제외할 범위

- 사용자가 강아지 위에 직접 붓으로 칠하는 기능
- 임의 사진을 무늬로 업로드하는 기능
- 실시간 천·머리카락 물리 효과
- 모든 RGB 값을 자유롭게 고르는 고급 색상 편집기
- 한 액세서리를 뼈대 변형으로 모든 전신 체형에 자동 맞추는 기능

첫 출시는 검수된 팔레트와 슬롯 기반 액세서리에 집중한다. 자유 색상은 명암과 가독성 검증 규칙이 마련된 뒤 확장한다.

## 17. 기대 결과

- 기본 강아지 자산은 30견종 × 16동작으로 고정된다.
- 염색 종류가 늘어도 강아지 PNG 수는 증가하지 않는다.
- 안경·리본·왕관은 공통 PNG와 위치 데이터만 추가하면 된다.
- 옷과 목도리는 소수 방향·체형 템플릿으로 확장할 수 있다.
- 웹과 Windows가 같은 카탈로그와 revision을 사용해 결과가 달라지는 문제를 줄인다.
- 새 꾸미기 상품을 추가할 때 필요한 작업량과 CDN 용량을 예측할 수 있다.

## 18. 관련 문서

- [공통 액세서리 에셋 구조](docs/ACCESSORY_ASSETS.md)
- [Ruby Round 16동작 제작·검증 파이프라인](docs/ruby-round-scenes-pipeline.md)
- [사이트 이미지와 CDN 관리](docs/SITE_ASSETS.md)
- [Windows 웹 연결과 동기화](docs/DESKTOP_SYNC.md)
