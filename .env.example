# 운영 환경은 이 루트 파일을 읽지 않습니다.
# AWS Elastic Beanstalk 설정: backend/.env.example
# Vercel 설정: frontend/.env.example
#
# 아래 두 값은 선택형 로컬 Docker Compose에서만 사용합니다.
POSTGRES_PASSWORD=replace-with-a-local-password
PUBLIC_SITE_URL=http://127.0.0.1:3000
