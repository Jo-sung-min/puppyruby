# Existing service tests use their own disposable H2 schemas. Migration tests opt in explicitly.
# These are test-only defaults; deployment configuration remains in main/application.yml.
spring.flyway.enabled=false
DB_SCHEMA=
S3_UPLOAD_ENABLED=false
DESKTOP_RELEASE_UPLOAD_ENABLED=false
MAIL_ENABLED=false
