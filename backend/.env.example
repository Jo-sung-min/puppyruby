# 로컬 H2 파일 DB는 별도 설정 없이 실행됩니다.
# PostgreSQL은 셸 환경 변수 또는 Docker Compose로 전달하세요.
DB_URL=jdbc:postgresql://localhost:5433/puppyruby
DB_USERNAME=puppyruby
DB_PASSWORD=replace-with-a-local-password
PORT=8081
